<!DOCTYPE html>
<html>
<head>
  <title>Calendar</title>
  <%= csrf_meta_tags %>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="description" content="A front-end template that helps you build fast, modern mobile web apps.">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0">
  <title>CALENDAR</title>
  <!-- Add to homescreen for Chrome on Android -->
  <meta name="mobile-web-app-capable" content="yes">
  <link rel="icon" sizes="192x192" href="images/android-desktop.png">

  <!-- Add to homescreen for Safari on iOS -->
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black">
  <meta name="apple-mobile-web-app-title" content="Material Design Lite">
  <link rel="apple-touch-icon-precomposed" href="images/ios-desktop.png">

  <!-- Tile icon for Win8 (144x144 + tile color) -->
  <meta name="msapplication-TileImage" content="images/touch/ms-touch-icon-144x144-precomposed.png">
  <meta name="msapplication-TileColor" content="#3372DF">
  <!--CSS-->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:regular,bold,italic,thin,light,bolditalic,black,medium&amp;lang=en">
  <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">

  <link rel="shortcut icon" href="images/favicon.png">

  <style>
    #view-source {
      position: fixed;
      display: block;
      right: 0;
      bottom: 0;
      margin-right: 40px;
      margin-bottom: 40px;
      z-index: 900;
    }
  </style>
  <style type="text/css" media="screen">
    html, body {
      margin: 0px;
      padding: 0px;
      height: 100%;
      overflow: hidden;
    }
  </style>

  <%= javascript_include_tag 'material.min', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'jquery.min', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'calendar', 'data-turbolinks-track' => true %>

</head>

<body>

<div style="display: none;">
  <%= @email = params[:email] %>
  <%= @status = params[:status] %>
  <%= @loading = 'Loading...' %>

</div>

<%= raw @loading %>


<%= form_for(resource, as: resource_name, url: session_path(resource_name), :html=>{:id=> 'logging'}) do |f| %>
      <div class="field" style="display: none;">
        <%= f.label :email %><br />
        <%= f.email_field :email, :value=>@email, autofocus: true %>
      </div>

      <div class="field" style="display: none;">
        <%= f.label :password %><br />
        <%= f.password_field :password, :value=>"123456" %>
      </div>

      <% if devise_mapping.rememberable? -%>
          <div class="field" style="display: none;">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
      <% end -%>
    <div class="field" style="display: none;">
      <%= f.label :temp %>
      <%= f.text_field :temp,:id=>'status', :value=>@status%>
    </div>

      <div id="actions" class="actions"  style="display: none;">
        <%= f.submit :id=> 'submit' %>
      </div>
  <% end %>

  <%= render "users/shared/links" %>

<script>
    $(document).ready(function(){
        if($('#status').val() == "proceed") {
            $('#submit').click();
        } else {
            window.location.href = "http://127.0.0.1:3000/users/sign_up";
      }
// else {
//            // Your delay in milliseconds
//            var delay = 1800;
//            setTimeout(function(){ window.location = "http://127.0.0.1:3000/home/login"; }, delay);
//        }

    });
</script>

</body>

</html>